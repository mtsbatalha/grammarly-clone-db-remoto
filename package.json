{
  "name": "grammarly-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Clone completo do Grammarly com extensão de navegador e dashboard web",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "npm run dev --workspace=@grammarly-clone/api",
    "dev:web": "npm run dev --workspace=@grammarly-clone/web",
    "build": "turbo run build",
    "start": "turbo run start",
    "start:all": "node scripts/start-all.js",
    "stop": "docker-compose -f docker-compose.dev.yml down",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:migrate": "npm run db:migrate --workspace=@grammarly-clone/api",
    "db:seed": "npm run db:seed --workspace=@grammarly-clone/api",
    "db:studio": "npm run db:studio --workspace=@grammarly-clone/api",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:dev:down": "docker-compose -f docker-compose.dev.yml down",
    "setup:linux": "chmod +x scripts/setup-linux.sh && ./scripts/setup-linux.sh",
    "setup:windows": "powershell -ExecutionPolicy Bypass -File scripts/setup-windows.ps1",
    "setup:nginx": "chmod +x scripts/setup-nginx-proxy.sh && ./scripts/setup-nginx-proxy.sh",
    "setup:nginx:windows": "powershell -ExecutionPolicy Bypass -File scripts/setup-nginx-proxy.ps1",
    "nginx:up": "docker-compose -f docker-compose.npm.yml up -d",
    "nginx:down": "docker-compose -f docker-compose.npm.yml down",
    "nginx:logs": "docker-compose -f docker-compose.npm.yml logs -f",
    "nginx:restart": "docker-compose -f docker-compose.npm.yml restart",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.0.0"
}
